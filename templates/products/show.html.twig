{% extends 'base.html.twig' %}

{% block title %}
	Products


{% endblock %}

{% block body %}

	<div class="container">
		<div class="grix xs4">
			<div class="col-xs2">
				<h3>{{product.marque}}</h3>
				<h1>{{product.designation}}</h1>
				<div class="txt-right">
					<h4>{{product.prix}}
						euros</h4>
					<h4>quantité disponible :
						{{product.stock}}</h4>
					<button class="btn  bg-cyan1 press" title="">
						<a href="{{ path('cart_add', {'id': product.id}) }}">
							<i class="fa fa-cart-plus" aria-hidden="true"></i>
							ajouter au panier
						</a>
					</button>
				</div>
				<h5>description :
				</h5>
				{{product.description}}
			</div>
			<div class="col-xs2 row-xs2 self-right pt-4">
				<img class=" p-2 rounded-2 responsive-media" src="{{ product.img }}" alt=""/>
			</div>
			<div class="col-xs4 my-2">
				<h5>details :
				</h5>
				{{product.details}}
			</div>

		</div>

	</div>


{% endblock %}
